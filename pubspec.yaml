name: ai_duplicate_removal
description: AI-powered duplicate contact removal app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_contacts: ^1.1.7+1
  file_picker: ^8.0.0+1

  permission_handler: ^11.0.1
  path_provider: ^2.1.1
  csv: ^5.0.2
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  crypto: ^3.0.3
  image: ^4.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true